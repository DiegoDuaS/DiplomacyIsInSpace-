-- The game's logical resolution, written once. The game always draws in these pixels.
-- scale and display only matter to conf.lua and the --debug editor viewport.
return { w = 640, h = 400, scale = 2, display = 1 }
